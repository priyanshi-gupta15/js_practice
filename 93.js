//que => Doubling the Numbers: Write a function double Numbers that takes an array of numbers and returns a new array where each number is doubled. Use the map method to solve this.

function double_number()
{
    let ans = array.map((number)=>2*number) 
    console.log(ans);
    
}

let array = [1,4,9,6,2,33]
double_number(array)